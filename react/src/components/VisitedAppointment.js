import React, { Component } from 'react';

const VisitedAppointment = props => {
  return (
    <div>
      // {props.date}
      // Reason: {props.reason}
      // Notes: {props.notes}
    </div>
  )
}

export default VisitedAppointment;
